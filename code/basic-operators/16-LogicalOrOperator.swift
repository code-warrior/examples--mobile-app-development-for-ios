var payingWithCash = false
var payingWithDigital = true

if payingWithCash || payingWithDigital {
  print("Proceeding with payment")
} else {
  print("Sorry, we only take cash or digital currency")
}
